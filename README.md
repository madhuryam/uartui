# uartui — A Modern Serial Port TUI

A modern, keyboard-driven TUI serial monitor for serial port communication, built in Rust.

Dual hex + ASCII views, auto-logging, user-defined protocol decoders, and session profiles. Works over SSH for field/remote debugging
